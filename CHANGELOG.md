# Changelog

## 0.2.0

- Add BM25 lexical retrieval and deterministic SimHash-LSH ANN candidate retrieval.
- Add immutable generation-versioned route tables with atomic ArcSwap hot reload.
- Add active health probes and per-route closed/open/half-open circuit state.
- Add Prometheus/OpenMetrics metrics, tracing spans, and optional OTLP export.
- Add structured clarification responses for ambiguous and high-impact requests.
- Add JSON Schema subset compatibility scoring and required-schema filtering.
- Add bounded online quality adaptation with explicit high-impact and fallback exclusions.
- Add ten-service Docker topology and a deterministic 1,000-scenario HTML benchmark.

## 0.1.0

- Initial hybrid keyword and embedding semantic API router.
